version: "3.9"

services:
  web:
    build: .
    container_name: evshowroom
    command: >
      sh -c "
      python manage.py migrate &&
      gunicorn EVShowroom.wsgi:application --bind 0.0.0.0:8000
      "
    ports:
      - "8000:8000"
    env_file:
      - .env